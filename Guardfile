guard 'coffeescript', :bare => true, :output => 'game/javascripts/' do
  watch(/^game\/javascripts\/(.*)\.js\.coffee/)
end

guard 'coffeescript', :output => 'spec/javascripts' do
  watch(/^spec\/javascripts\/(.*)\.js\.coffee/)
end
